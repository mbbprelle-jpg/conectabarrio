# Variables para Coolify o desarrollo local (no commitear .env con secretos)
# Guía: deploy/brevo/README.md

# --- Entorno (production | local) ---
# production: no muestra errores PHP al usuario; los registra en log del servidor
APP_ENV=production

# --- Base de datos ---
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=conectabarrio

# --- SMTP (Brevo) ---
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=ac86ca001@smtp-brevo.com
SMTP_PASS=
SMTP_FROM_EMAIL=contacto@conectabarrio.cl
SMTP_FROM_NAME=ConectaBarrio
SMTP_ENCRYPTION=tls
