<IfModule mod_rewrite.c>
    RewriteEngine On
    # Redirigir la raíz a /public
    RewriteRule ^$ public/ [L]
    
    # Redirigir el resto de las peticiones a /public/ y mantener los argumentos
    RewriteRule (.*) public/$1 [L]
</IfModule>
